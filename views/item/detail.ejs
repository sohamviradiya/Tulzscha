<!DOCTYPE html>
<html lang="en">
<%- include('../head',{ title: locals.title }) -%>

     <body>
          <%- include('../header') -%>
               <div class="container d-flex flex-column justify-content-evenly align-items-center text-center">
                    <h1 class="display-1 p-5">
                         <%= item.id %>
                    </h1>
                    <h2 class="display-4 p-3">
                         <%= item.product.name %>
                    </h2>
                    <h3 class="display-5 p-2">
                         Availability: <%= item.status %>
                    </h3>
                    <h3 class="display-5 p-2">
                         Manufactring Date: <%= String(item.manufactringDate).slice(4,16) %>
                    </h3>
                    <a class="btn btn-primary mb-5" href="/catalog/item/<%=item.id%>/update"> Update </a>
                    <a class="btn btn-danger mb-5" href="/catalog/item/<%=item.id%>/delete"> Delete </a>
               </div>
     </body>

</html>